<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ddmpricelessbrains">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DDM">
        <activity android:name=".ui.CardActivity"></activity>
        <activity android:name=".ui.LojaActivity" />
        <activity android:name=".ui.ClientesActivity" />
        <activity android:name=".ui.CatalogoActivity" />
        <activity android:name=".ui.AgendaActivity" />
        <activity android:name=".ui.SettingsActivity" />
        <activity android:name=".ui.SecondFragment" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DDM.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>